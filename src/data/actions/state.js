export const setPlayers = (noPlayers) => {
  return {
    type: "UPDATE_NO",
    noPlayers: noPlayers,
  };
};

export const setNames = (noPlayers) => {
  return {
    type: "UPDATE_NO",
    noPlayers,
  };
};
